---
active: true
defect-category: ?c5-defect-0
derived: false
failed-steps: '0'
level: 2.18
links:
- TST-059: uJ0eit0envCMGqi5kShLlndLY3X9ejLLVSz_0837Mqo=
normative: true
not-executed-steps: '0'
passed-steps: '11'
ref: ''
release-version: '2.0'
reviewed: eSolia1hMQeij-HTvaXaobLEFG1cHyZ1zXS96NXyJDs=
test-date: 2026-08-19
tester: AAT
verification-method: T
---

# TCER: GUI: Unenroll, undeploy, and deregister the `log_volume` scenario

## Relevant test environment and configuration details

- Software deviations: aligned with test case specification, with controller node running GNU/Linux (Ubuntu) and second test node running Debian GNU/Linux 13 (trixie); Wazuh manager/agent version `4.14.1`
- Hardware deviations: aligned with test case specification

## Test execution results

Here we report the results in terms of step-wise alignments or deviations with respect to the expected outcome of the covered test case.

**Test case step 1**: GUI started (or already running) via `./radar.sh gui`; reachable in the browser at the printed host:port, with **Scenario Management**, **Agent Management**, **Group Management**, and **Status** tabs present under Deploy — ?c5-defect-0

**Test case step 2**: Wazuh Dashboard **Agents management → Summary** checked before changes; agent shows both `default` and `log_volume` groups — ?c5-defect-0

**Test case step 3**: Agent unenrolled from scenario `Log Volume Growth` via **Deploy → Group Management → Unenroll from groups**; streamed output ended with `=== SUCCESS: group unassignment for 'log_volume' completed ===` — ?c5-defect-0

**Test case step 4**: Wazuh Dashboard **Agents management → Summary** checked again; agent now shows only the `default` group — ?c5-defect-0

**Test case step 5**: Agent re-enrolled via **Deploy → Group Management → Assign group**; streamed output ended with `=== SUCCESS: group assignment for 'log_volume' completed ===`, and the agent is re-assigned to both `default` and `log_volume` — ?c5-defect-0

**Test case step 6**: Scenario `Log Volume Growth` undeployed via **Deploy → Scenario Management → Undeploy scenario**; streamed output ended with `=== SUCCESS: undo for 'log_volume' completed ===`, with no `error` key present in either printed result — ?c5-defect-0

**Test case step 7**: Health check run for the Log Volume Growth scenario in the **Status** tab

- (a) the four `wazuh-*` daemon checks and the OpenSearch/Webhook checks all still reported `OK` — ?c5-defect-0
- (b) the five checks for active-response wiring, group config, and the three decoder/rule files reported `FAIL` — ?c5-defect-0

**Test case step 8**: On the manager host, `docker exec wazuh.manager grep -c "RADAR: log_volume" /var/ossec/etc/ossec.conf` returned `0` — ?c5-defect-0

**Test case step 9**: Wazuh Dashboard **Agents management → Summary** checked once more; agent shows only the `default` group — ?c5-defect-0

**Test case step 10**: Agent deregistered via **Deploy → Agent Management → Deregister agent**; streamed output ended with JSON result followed by `=== SUCCESS: deregistration for '<agent-name>' completed ===` — ?c5-defect-0

**Test case step 11**: Wazuh Dashboard **Agents management → Summary** checked; the agent no longer appears in the agents list — ?c5-defect-0

### Defect summary description

Defect-free test execution, i.e., defect category: ?c5-defect-0

### Text execution evidence

See linked files (if any), e.g., screenshots, logs, etc.

### Comments

All steps completed successfully, matching the expected outcome in [TST-059](./TST.html#TST-059). Cleanup performed via the **Teardown** tab in the GUI and `uninstall-agent.sh --purge-data` on the agent host.

## Guide

- Defect category: ?c5-defect-0; ?c5-defect-1; ?c5-defect-2; ?c5-defect-3; ?c5-defect-4
- Verification method (VM): Test (T), Review of design (R), Inspection (I), Analysis (A)