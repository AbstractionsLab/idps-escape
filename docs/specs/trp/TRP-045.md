---
active: true
defect-category: ?c5-defect-0
derived: false
failed-steps: '0'
level: 2.14
links:
- TST-055: SGS2ySfrA9hJ8Ogbm3Qq2X_U5etDDYxImvDaR1ymqGA=
normative: true
not-executed-steps: '0'
passed-steps: '8'
ref: ''
release-version: '2.0'
reviewed: Q5skg5TAx6vvu2IN3k8WSXai6Dm88ncj2LQzF92mg_w=
test-date: 2026-08-18
tester: AAT
verification-method: T
---

# TCER: GUI: Build & validate `suspicious_login` scenario

## Relevant test environment and configuration details

- Software deviations: aligned with test case specification, with controller node running GNU/Linux (Ubuntu) and second test node running Debian GNU/Linux 13 (trixie); Wazuh manager/agent version `4.14.1`; DECIPHER Stack (DECIPHER, FlowIntel, MISP) deployed per SATRAP-DL `v1.0`
- Hardware deviations: aligned with test case specification

## Test execution results

Here we report the results in terms of step-wise alignments or deviations with respect to the expected outcome of the covered test case.

**Test case step 1**: `.env` prepared under `radar` with the required variables set, including `MAXMIND_LICENSE_KEY` — ?c5-defect-0

**Test case step 2**: GUI started via `./radar.sh gui`; reachable in the browser at the printed host:port, with **Build & deploy**, **Onboard Agent**, and **Group Enrollment** tabs present; `.venv` created on first run — ?c5-defect-0

**Test case step 3**: Scenario `suspicious_login` built and deployed via **Deploy → Build & deploy**

- (a) command completed with `=== SUCCESS` in its output — ?c5-defect-0
- (b) `GeoLite2-City.mmdb` and `GeoLite2-ASN.mmdb` confirmed present under `/var/ossec/etc/radar/` on the manager — ?c5-defect-0

**Test case step 4**: Onboarding token minted for scenario `suspicious_login`; ready-to-run `bootstrap-agent.sh` command printed — ?c5-defect-0

**Test case step 5**: `bootstrap-agent.sh` executed on the agent host; command completed with `=== SUCCESS ===` in its output — ?c5-defect-0

**Test case step 6**: Agent enrolled via **Group Enrollment**; Wazuh Dashboard **Agents management → Summary** shows the agent assigned groups `suspicious_login` and `radar_shared` — ?c5-defect-0

**Test case step 7**: Health check run for the Suspicious Login scenario in the **Status** tab; no `FAIL` lines present — ?c5-defect-0

**Test case step 8**: `suspicious_login.py` simulation run directly on the agent host

- (a) script completed without error, printing "suspicious_login simulation completed" — ?c5-defect-0
- (b) `enriched_auth.log` on the manager showed authentication events enriched with RADAR columns within 1 minute — ?c5-defect-0
- (c) Wazuh rule `210021` (RADAR SuspiciousLogin: impossible travel) appeared in Wazuh Dashboard **Discovery** — ?c5-defect-0

### Defect summary description

Defect-free test execution, i.e., defect category: ?c5-defect-0

### Text execution evidence

See linked files (if any), e.g., screenshots, logs, etc.

### Comments

All steps completed successfully, matching the expected outcome in [TST-055](./TST.html#TST-055). Cleanup performed via the **Teardown** tab in the GUI and `uninstall-agent.sh --purge-data` on the agent host.

## Guide

- Defect category: ?c5-defect-0; ?c5-defect-1; ?c5-defect-2; ?c5-defect-3; ?c5-defect-4
- Verification method (VM): Test (T), Review of design (R), Inspection (I), Analysis (A)