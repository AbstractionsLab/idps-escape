---
active: true
defect-category: ?c5-defect-0
derived: false
failed-steps: '0'
level: 2.15
links:
- TST-056: Ny6Eu4G2RHN-aOXvw7JI4vVqFFB4paJtrXdn6EgDz04=
normative: true
not-executed-steps: '0'
passed-steps: '8'
ref: ''
release-version: '2.0'
reviewed: sFn_UjI9QP0KvhUlwTscuz8erR2tqrgc_A4fFOUQPW8=
test-date: 2026-08-18
tester: AAT
verification-method: T
---

# TCER: GUI: Build & validate `geoip_detection` scenario

## Relevant test environment and configuration details

- Software deviations: aligned with test case specification, with controller node running GNU/Linux (Ubuntu) and second test node running Debian GNU/Linux 13 (trixie); Wazuh manager/agent version `4.14.1`; DECIPHER Stack (DECIPHER, FlowIntel, MISP) deployed per SATRAP-DL `v1.0`
- Hardware deviations: aligned with test case specification

## Test execution results

Here we report the results in terms of step-wise alignments or deviations with respect to the expected outcome of the covered test case.

**Test case step 1**: `.env` prepared under `radar` with the required variables set, including `MAXMIND_LICENSE_KEY` — ?c5-defect-0

**Test case step 2**: GUI started via `./radar.sh gui`; reachable in the browser at the printed host:port, with **Build & deploy**, **Onboard Agent**, and **Group Enrollment** tabs present — ?c5-defect-0

**Test case step 3**: Scenario `geoip_detection` built and deployed via **Deploy → Build & deploy**

- (a) command completed with `=== SUCCESS` in its output — ?c5-defect-0
- (b) `GeoLite2-City.mmdb` and `GeoLite2-ASN.mmdb` confirmed present under `/var/ossec/etc/radar/` on the manager — ?c5-defect-0

**Test case step 4**: Onboarding token minted for scenario `geoip_detection`; ready-to-run `bootstrap-agent.sh` command printed — ?c5-defect-0

**Test case step 5**: `bootstrap-agent.sh` executed on the agent host; command completed with `=== SUCCESS ===` in its output — ?c5-defect-0

**Test case step 6**: Agent enrolled via **Group Enrollment**; Wazuh Dashboard **Agents management → Summary** shows the agent assigned groups `geoip_detection` and `radar_shared` — ?c5-defect-0

**Test case step 7**: Health check run for the GeoIP Detection scenario in the **Status** tab; no `FAIL` lines present — ?c5-defect-0

**Test case step 8**: `geoip_detection.py` simulation run directly on the agent host

- (a) script completed without error, printing "geoip_detection simulation completed" — ?c5-defect-0
- (b) within 2 minutes, rule `100900` appeared in Wazuh Dashboard **Discovery**, `radar_ar.py` wrote an active-response entry to `active-responses.log`, an email notification was delivered, and `enrichment_errors.log` had no new entries — ?c5-defect-0

### Defect summary description

Defect-free test execution, i.e., defect category: ?c5-defect-0

### Text execution evidence

See linked files (if any), e.g., screenshots, logs, etc.

### Comments

All steps completed successfully, matching the expected outcome in [TST-056](./TST.html#TST-056). Cleanup performed via the **Teardown** tab in the GUI and `uninstall-agent.sh --purge-data` on the agent host.

## Guide

- Defect category: ?c5-defect-0; ?c5-defect-1; ?c5-defect-2; ?c5-defect-3; ?c5-defect-4
- Verification method (VM): Test (T), Review of design (R), Inspection (I), Analysis (A)