---
active: true
defect-category: ?c5-defect-0
derived: false
failed-steps: '0'
level: 2.12
links:
- TST-053: 9yWp9ctZtERYj0CzFR409Zim9enE_41-J6kPJXbigGE=
normative: true
not-executed-steps: '0'
passed-steps: '3'
ref: ''
release-version: '1.2'
reviewed: rm_AnbXI8fwWKdPqR7qh8frnEF1Lkx8WcWMwj1ZANZU=
test-date: 2026-07-29
tester: GDU
verification-method: T
---

# TCER: Validate default scenario rules detection

## Relevant test environment and configuration details

- Software deviations: aligned with test case specification, with controller node running GNU/Linux (Dockerized C5-DEC deployment environment)
- Hardware deviations: aligned with test case specification

## Test execution results

Here we report the results in terms of step-wise alignments or deviations with respect to the expected outcome of the covered test case. The executed command was as follows:
```
sudo ./build-radar.sh suspicious_login --manager local --agent remote --manager_exists false --ssh-key /home/<USER>/.ssh/id_ed25519
```

#### In Wazuh Manager

**Test case step 2.1:** RADAR rules for the default scenario are deployed

- ?c5-defect-0: rule IDs 100400-100405 are present in `/var/ossec/etc/rules/`.

**Test case step 2.2:** `ar.yaml` reflects the configured groups

- ?c5-defect-0: the `default:` block lists `rule_ids: ["100401","100402","100404","100405","23503","23504","23505"]` and `rule_groups: ["authentication_failures"]` as configured.

**Test case step 2.3:** The active-response binding in `ossec.conf` includes both the exact IDs and the two groups

- ?c5-defect-0: `<rules_id>` lists `100401,100402,100404,100405,23503,23504,23505` and `<rules_group>` lists `authentication_failures,`, bound to `radar_ar_default`.

**Test case step 2.4:** Wazuh Manager is running after restart

- ?c5-defect-0: the status output indicates the manager is `running` with no error messages.

#### Post-test cleanup

**Test case step 3:** Check the cleanup after successful validations, the command output should be empty

- ?c5-defect-0: `docker ps` output was empty.

### Defect summary description

?c5-defect-0

### Text execution evidence

See linked files (if any), e.g., screenshots, logs, etc.

### Comments

Any additional informative details not fitting in the above sections.

## Guide

- Defect category: ?c5-defect-0; ?c5-defect-1; ?c5-defect-2; ?c5-defect-3; ?c5-defect-4
- Verification method (VM): Test (T), Review of design (R), Inspection (I), Analysis (A)