---
active: true
defect-category: ?c5-defect-3
derived: false
failed-steps: '0'
level: 2.17
links:
- TST-058: z4N5aho4MuQqStxVV470Ped2Va9WBRhLlgb7BnLRlnI=
normative: true
not-executed-steps: '0'
passed-steps: '9'
ref: ''
release-version: '2.0'
reviewed: fbTee6Sm-KpvUok-amHBG3QOvjFQ8AvoTQHuD9838v4=
test-date: 2026-08-19
tester: AAT
verification-method: T
---

# TCER: GUI: Build & validate `log_volume` scenario

## Relevant test environment and configuration details

- Software deviations: aligned with test case specification, with controller node running GNU/Linux (Ubuntu) and second test node running Debian GNU/Linux 13 (trixie); Wazuh manager/agent version `4.14.1`
- Hardware deviations: aligned with test case specification

## Test execution results

Here we report the results in terms of step-wise alignments or deviations with respect to the expected outcome of the covered test case.

**Test case step 1**: `.env` prepared under `radar` with the required variables set — ?c5-defect-0

**Test case step 2**: GUI started via `./radar.sh gui`; reachable in the browser at the printed host:port, with **Build & deploy**, **Onboard Agent**, **Group Enrollment**, and **Run Anomaly Detector** tabs present; `.venv` created on first run — ?c5-defect-0

**Test case step 3**: Scenario `log_volume` built and deployed via **Deploy → Build & deploy**

- (a) command completed with `=== SUCCESS` in its output — ?c5-defect-0
- (b) the `radar-log-volume` index template was confirmed applied on the OpenSearch cluster — ?c5-defect-0

**Test case step 4**: Onboarding token minted for scenario `log_volume`; ready-to-run `bootstrap-agent.sh` command printed — ?c5-defect-0

**Test case step 5**: `bootstrap-agent.sh` executed on the agent host; command completed with `=== SUCCESS ===` in its output — ?c5-defect-0

**Test case step 6**: Agent enrolled via **Group Enrollment**; Wazuh Dashboard **Agents management → Summary** shows the agent assigned group `log_volume` — ?c5-defect-0

**Test case step 7**: `Log Volume Growth` detector run via **Deploy → Run Anomaly Detector** with the ingest option checked

- (a) streamed output completed without an `[ERROR]` line, reporting the detector was created and started — ?c5-defect-0
- (b) the `wazuh-ad-log-volume-*` index confirmed to contain more than 0 documents (`docs.count`) — ?c5-defect-0

**Test case step 8**: Health check run for the Log Volume Growth scenario in the **Status** tab; no `FAIL` lines present — ?c5-defect-0

**Test case step 9**: `log_volume.py` simulation run directly on the agent host

- (a) script completed without error, printing "log_volume simulation completed" — ?c5-defect-0
- (b) A spike or anomaly event is detected and visible on the log volume detector page of the Anomaly Detection dashboard of Wazuh. — ?c5-defect-0
- (c) active-response mitigation commands logged in `active-responses.log` — ?c5-defect-3
- (d) rule `100309` (LogVolume-Growth-Detected) fired in Wazuh Dashboard **Discovery**, and an email notification was received — ?c5-defect-3

### Defect summary description

Defect-free test execution, i.e., defect category: ?c5-defect-3

### Text execution evidence

See linked files (if any), e.g., screenshots, logs, etc.

### Comments

See linked files (if any), e.g., screenshots, logs, etc.

## Guide

- Defect category: ?c5-defect-0; ?c5-defect-1; ?c5-defect-2; ?c5-defect-3; ?c5-defect-4
- Verification method (VM): Test (T), Review of design (R), Inspection (I), Analysis (A)