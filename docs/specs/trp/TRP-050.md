---
active: true
defect-category: ?c5-defect-0
derived: false
failed-steps: '0'
level: 2.19
links:
- TST-060: YoyseTFoOUHg0plhc9nCTRj0zxPmWwVrwC8leSBolBo=
normative: true
not-executed-steps: '0'
passed-steps: '10'
ref: ''
references_content_fingerprint: {}
release-version: '2.1'
reviewed: MsAgIEuG4QvIoxD-PKMuIdjjw2uifBbnWSMlxFvGEDs=
test-date: 2026-09-21
tester: AAT
verification-method: T
---

# TCER: GUI: Deploy scenario on pre-existing SN Wazuh

Note that in the instructions below, the term "right" (e.g. the right scenario names) refers to the name of each scenario being tested for and its associated artefacts; this is because this test case validates a certain property for multiple RADAR scenarios.

## Relevant test environment and configuration details

- Software deviations: aligned with test case specification, with controller node running GNU/Linux (Ubuntu) and a pre-existing single-node Wazuh v4.14.4 manager/indexer/dashboard stack brought up independently via Wazuh's official docker-compose files
- Hardware deviations: aligned with test case specification

## Test execution results

Here we report the results in terms of step-wise alignments or deviations with respect to the expected outcome of the covered test case.

**Test case step 1**: `.env` prepared on the controller host, pointing at the existing Wazuh stack's addresses — ?c5-defect-0

**Test case step 2**: GUI started via `./radar.sh gui`; reachable in the browser at the printed host:port — ?c5-defect-0

**Test case step 3**: Infrastructure page shows the manager, indexer, and dashboard registered with the existing stack's real container names

- (a) `WEBHOOK_INTERNAL_PORT` confirmed free before use; `WEBHOOK_URL` and the registered webhook port match it — ?c5-defect-0

**Test case step 4**: **Test link** run on the Infrastructure page; reports all three components reachable — ?c5-defect-0

**Test case step 5**: `volumes.yml` confirmed to have a complete entry keyed by the manager's exact container name, covering all six required container paths (`/var/ossec/etc`, `/var/ossec/logs`, `/var/ossec/integrations`, `/var/ossec/active-response/bin`, `/etc/filebeat`, `/usr/share/filebeat/module/wazuh/archives/ingest/pipeline.json`) — ?c5-defect-0

**Test case step 6**: Scenarios `suspicious_login`, `log_volume`, `scanning_detection` and `geoip_detection` deployed via **Deployment → Scenario Management → Deploy**; command completed with `=== SUCCESS` in its output — ?c5-defect-0

**Test case step 7**: Token minted for scenarios listed in the previous steps via **Deployment → Agent Management**; ready-to-run `bootstrap-agent.sh` command printed, carrying the group name — ?c5-defect-0

**Test case step 8**: `bootstrap-agent.sh` downloaded and run under `sudo` on the agent host, exactly as printed; command completed with `=== SUCCESS ===` in its output — ?c5-defect-0

**Test case step 9**: Agent IP/name entered and scenario selected via **Deployment → Group Management → Assign group**; Wazuh Dashboard **Agents management → Summary** shows the agent assigned the right group name — ?c5-defect-0

**Test case step 10**: Remaining steps of respective scenarios listed in [TST-060](./TST.html#TST-060) completed against the right container names

- (a) health check run for the Suspicious Login scenario in the **Status** tab; no `FAIL` lines present — ?c5-defect-0
- (b) scenario simulation run on the agent host, completing without error and printing "<scenario> simulation completed"; rule, log, and email checks all passed — ?c5-defect-0

### Defect summary description

Defect-free test execution, i.e., defect category: ?c5-defect-0

### Text execution evidence

See linked files (if any), e.g., screenshots, logs, etc.

### Comments

All steps completed successfully, matching the expected outcome in [TST-060](./TST.html#TST-060). Cleanup performed via **Deployment → Teardown** in the GUI and `uninstall-agent.sh --purge-data` on the agent host.

## Guide

- Defect category: ?c5-defect-0; ?c5-defect-1; ?c5-defect-2; ?c5-defect-3; ?c5-defect-4
- Verification method (VM): Test (T), Review of design (R), Inspection (I), Analysis (A)