---
active: true
defect-category: ?c5-defect-0
derived: false
failed-steps: '0'
level: '2.10'
links:
- TST-051: 030tF_Er1QpXgjj1xqvJp9_n8RR0a58M2ZoQp07UacY=
normative: true
not-executed-steps: '0'
passed-steps: '3'
ref: ''
release-version: '1.1'
reviewed: hlLkpn6MZneJ-XAYde_C76C1_6qooM_4k3pugWoMm8U=
test-date: 2026-07-29
tester: GDU
verification-method: T
---

# TCER: Build scanning detection

## Relevant test environment and configuration details

- Software deviations: aligned with test case specification, with controller node running GNU/Linux (Dockerized C5-DEC deployment environment)
- Hardware deviations: aligned with test case specification

## Test execution results

Here we report the results in terms of step-wise alignments or deviations with respect to the expected outcome of the covered test case. The executed command was as follows:
```
sudo ./build-radar.sh scanning_detection --manager local --agent remote --manager_exists false --ssh-key /home/<USER>/.ssh/id_ed25519
```

#### In Wazuh Manager

**Test case step 2.1:** RADAR scanning detection rules are deployed

- ?c5-defect-0: `a4-scanning-detection.xml` exists, owned by `root:wazuh`, mode `0640`.

**Test case step 2.2:** Scanning detection rules contain all expected rule IDs

- ?c5-defect-0: rules 100800 through 100805 are present in `a4-scanning-detection.xml`.

**Test case step 2.3:** Scanning detection rules are bound to active responses

- ?c5-defect-0: scanning rule IDs reference the `radar_ar_scanning` `<active-response>` block triggering `radar_ar.py`.

**Test case step 2.4:** Agent is registered with the manager

- ?c5-defect-0: an entry exists for each agent.

**Test case step 2.5:** Agent configurations for scanning detection are in place

- ?c5-defect-0: the `<!-- RADAR: scanning_detection agent_config -->` block exists and contains Apache and Nginx log source entries with `log_format apache`.

**Test case step 2.6:** Wazuh Manager is running after restart

- ?c5-defect-0: the status output indicates the manager is `running` with no error messages.

#### In endpoints running agents

**Test case step 2.7:** Wazuh agent is active and connected to the manager

- ?c5-defect-0: the agent service is active.

**Test case step 2.8:** Apache or Nginx is running and writing to the expected log path

- ?c5-defect-0: the log file(s) exist and contain recent access log entries in combined log format.

### Defect summary description

?c5-defect-0

### Text execution evidence

See linked files (if any), e.g., screenshots, logs, etc.

### Comments

Any additional informative details not fitting in the above sections.

## Guide

- Defect category: ?c5-defect-0; ?c5-defect-1; ?c5-defect-2; ?c5-defect-3; ?c5-defect-4
- Verification method (VM): Test (T), Review of design (R), Inspection (I), Analysis (A)