---
active: true
defect-category: ?c5-defect-0
derived: false
failed-steps: '0'
level: 2.16
links:
- TST-057: in7ESMBPoPsaexkoMjsw1hf6nugLDRdpO5mxDSlhWZE=
normative: true
not-executed-steps: '0'
passed-steps: '8'
ref: ''
release-version: '2.0'
reviewed: Z3Pkl3ABc4yRWo6N8rxmiLNXPUJJiFKaBOwocFExEUQ=
test-date: 2026-08-18
tester: AAT
verification-method: T
---

# TCER: GUI: Build & validate `scanning_detection` scenario

## Relevant test environment and configuration details

- Software deviations: aligned with test case specification, with controller node running GNU/Linux (Ubuntu) and second test node running Debian GNU/Linux 13 (trixie); Wazuh manager/agent version `4.14.1`; Apache/Nginx log sources configured on the targeted agent host
- Hardware deviations: aligned with test case specification

## Test execution results

Here we report the results in terms of step-wise alignments or deviations with respect to the expected outcome of the covered test case.

**Test case step 1**: `.env` prepared under `radar` with the required variables set — ?c5-defect-0

**Test case step 2**: GUI started via `./radar.sh gui`; reachable in the browser at the printed host:port, with **Build & deploy**, **Onboard Agent**, and **Group Enrollment** tabs present; `.venv` created on first run — ?c5-defect-0

**Test case step 3**: Scenario `scanning_detection` built and deployed via **Deploy → Build & deploy**

- (a) command completed with `=== SUCCESS` in its output — ?c5-defect-0
- (b) `ossec.conf` on the manager confirmed to contain the marked `RADAR: scanning_detection` block — ?c5-defect-0

**Test case step 4**: Onboarding token minted for scenario `scanning_detection`; ready-to-run `bootstrap-agent.sh` command printed — ?c5-defect-0

**Test case step 5**: `bootstrap-agent.sh` executed on the agent host; command completed with `=== SUCCESS ===` in its output — ?c5-defect-0

**Test case step 6**: Agent enrolled via **Group Enrollment**; Wazuh Dashboard **Agents management → Summary** shows the agent assigned group `scanning_detection` — ?c5-defect-0

**Test case step 7**: Health check run for the Scanning Detection scenario in the **Status** tab; no `FAIL` lines present — ?c5-defect-0

**Test case step 8**: `scanning_detection.py` simulation run directly on the agent host

- (a) script completed without error, printing "scanning_detection simulation completed" — ?c5-defect-0
- (b) within 2 minutes, scanning rules per `radar/scenarios/rules/scanning_detection/` (`100800`–`100804`) appeared in Wazuh Dashboard **Discovery** — ?c5-defect-0

### Defect summary description

Defect-free test execution, i.e., defect category: ?c5-defect-0

### Text execution evidence

See linked files (if any), e.g., screenshots, logs, etc.

### Comments

All steps completed successfully, matching the expected outcome in [TST-057](./TST.html#TST-057). Cleanup performed via the **Teardown** tab in the GUI and `uninstall-agent.sh --purge-data` on the agent host.

## Guide

- Defect category: ?c5-defect-0; ?c5-defect-1; ?c5-defect-2; ?c5-defect-3; ?c5-defect-4
- Verification method (VM): Test (T), Review of design (R), Inspection (I), Analysis (A)