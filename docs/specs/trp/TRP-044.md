---
active: true
defect-category: ?c5-defect-0
derived: false
failed-steps: '0'
level: 2.13
links:
- TST-054: IvKci4lVssZKtSFtSgSvTHixHP18XmC5mOar36xmCKw=
normative: true
not-executed-steps: '0'
passed-steps: '1'
ref: ''
release-version: '1.2'
reviewed: tHfem_EbhAlNrqXNHfOjkha50hwweI9ek9jxiF8KCTQ=
test-date: 2026-07-29
tester: GDU
verification-method: T
---

# TCER: DECIPHER-RADAR detection validation for Default rules

## Relevant test environment and configuration details

- Software deviations: aligned with test case specification, with controller node running GNU/Linux (Dockerized C5-DEC deployment environment)
- Hardware deviations: aligned with test case specification

## Test execution results

Here we report the results in terms of step-wise alignments or deviations with respect to the expected outcome of the covered test case. The executed command was as follows:
```
sudo ./simulate-radar.sh default --agent remote --ssh-key /home/<USER>/.ssh/id_ed25519
```

**Test case step 1.1:** Based on syscollector frequency after 60 minutes, at least one alert of rule IDs `23503`, `23504`, or `23505` is triggered for the remote agent

- ?c5-defect-0: 3 emails with `rule.id` 23504, 2 emails with `rule.id` 23505, and 1 email with `rule.id` 23503 are received

**Test case step 1.2:** In Wazuh manager, alert tier verified via `grep "Risk computed" /var/ossec/logs/active-responses.log | tail -1`

- Tier 1:
      - ?c5-defect-0: email received at `EMAIL_TO` with risk score, tier, and FlowIntel case URL.
      - ?c5-defect-0: FlowIntel case created and accessible at the URL included in the email.
      - ?c5-defect-0: FlowIntel case contains relevant information about the vulnerability detection event and a breakdown of the risk score including the assigned tier.
      - ?c5-defect-0: FlowIntel case is tagged with low priority.
      - ?c5-defect-0: no mitigation command in `/var/ossec/logs/active-responses.log`.

### Defect summary description

?c5-defect-0

### Text execution evidence

See linked files (if any), e.g., screenshots, logs, etc.

### Comments

Any additional informative details not fitting in the above sections.

## Guide

- Defect category: ?c5-defect-0; ?c5-defect-1; ?c5-defect-2; ?c5-defect-3; ?c5-defect-4
- Verification method (VM): Test (T), Review of design (R), Inspection (I), Analysis (A)