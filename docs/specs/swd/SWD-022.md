---
active: true
derived: false
level: 1.1
links:
- LARC-019: kKL-jBTSPFGJNyjZsMJtbzThwF3EHcVm3QvuCmpRfuw=
- LARC-020: 9fctuIygVtzkkKgxXQTX9RiAMNwdxcXjN_s_jcGo8Aw=
normative: true
ref: ''
reviewed: 9tcHQHxWZD1FMfyCt5Vb97XyDFo8WbAKCB7SW2T5eWs=
---

# SONAR class structure and relationships

## Class diagram overview

The SONAR subsystem employs a modular class structure organized around configuration management, data access, and processing pipelines.

### Core configuration classes

```plantuml
@startuml
class PipelineConfig {
  +WazuhIndexerConfig wazuh
  +MVADConfig mvad
  +FeatureConfig features
  +DebugConfig debug
  +ShippingConfig shipping
  +str model_path
}

class WazuhIndexerConfig {
  +str base_url
  +str username
  +str password
  +bool verify_ssl
  +str alerts_index_pattern
}

class MVADConfig {
  +int sliding_window
  +str device
  +Dict extra_params
  +to_params() Dict
}

class FeatureConfig {
  +Sequence<str> numeric_fields
  +int bucket_minutes
  +int categorical_top_k
  +bool derived_features
}

PipelineConfig *-- WazuhIndexerConfig
PipelineConfig *-- MVADConfig
PipelineConfig *-- FeatureConfig
@enduml
```

### Scenario management classes

```plantuml
@startuml
class UseCase {
  +str name
  +str description
  +Optional<str> model_name
  +TrainingScenario training
  +DetectionScenario detection
  +from_yaml(path) UseCase
  +to_yaml(path) void
}

class TrainingScenario {
  +int lookback_hours
  +List<str> numeric_fields
  +int bucket_minutes
  +int sliding_window
}

class DetectionScenario {
  +str mode
  +int lookback_minutes
  +float threshold
  +int min_consecutive
}

UseCase *-- TrainingScenario
UseCase *-- DetectionScenario
@enduml
```

## Configuration dataclasses summary

**Implementation**: [sonar/config.py](sonar/config.py)

| Dataclass | Key Fields | Purpose |------|
|-----------|------------|---------|------|
| `PipelineConfig` | wazuh, mvad, features, debug, shipping | Top-level pipeline configuration |
| `WazuhIndexerConfig` | base_url, username, password, alerts_index_pattern | Wazuh Indexer connection |
| `MVADConfig` | sliding_window, device, extra_params | MVAD engine parameters |
| `FeatureConfig` | numeric_fields, bucket_minutes, categorical_top_k | Feature extraction configuration |
| `DebugConfig` | enabled, data_dir, training_file, detection_file | Debug mode data sources |
| `ShippingConfig` | enabled, scenario_id, indexer_url, index_name | Anomaly shipping configuration |
| `UseCase` | name, description, training, detection, shipping | Scenario definition |
| `TrainingScenario` | lookback_hours, numeric_fields, sliding_window | Training parameters |
| `DetectionScenario` | mode, lookback_minutes, threshold, min_consecutive | Detection parameters |

## Design patterns

| Pattern | Application | Benefit |------|
|---------|-------------|---------|------|
| **Dataclasses** | Configuration objects | Type safety, immutability, validation |
| **Factory Method** | `UseCase.from_yaml()` | Encapsulates YAML parsing logic |
| **Strategy** | Data providers (Wazuh/Local) | Interchangeable data sources |
| **Facade** | Engine wrapper | Simplifies MVAD library interface |

## Related documentation

- Complete class diagram: [docs/manual/sonar_docs/uml-diagrams.md](docs/manual/sonar_docs/uml-diagrams.md#class-diagram)
- Architecture guide: [docs/manual/sonar_docs/architecture.md](docs/manual/sonar_docs/architecture.md#module-structure)